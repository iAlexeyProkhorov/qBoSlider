@model IWidgetZoneSearchModel
@{
    const string hideSearchBlockAttributeName = "Baroque.qBoSlider.WidgetZone.HideSearchBlock";
    var hideSearchBlock = await genericAttributeService.GetAttributeAsync<bool>(await workContext.GetCurrentCustomerAsync(), hideSearchBlockAttributeName);
}

<div class="card card-default card-search">
    <div class="card-body">
        <div class="row search-row @(!hideSearchBlock ? "opened" : "")" data-hideAttribute="@hideSearchBlockAttributeName">
            <div class="search-text">@T("Admin.Common.Search")</div>
            <div class="icon-search"><i class="fa fa-search" aria-hidden="true"></i></div>
            <div class="icon-collapse"><i class="fa fa-angle-down" aria-hidden="true"></i></div>
        </div>

        <div class="search-body @(hideSearchBlock ? "closed" : "")">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group row">
                        <div class="col-md-4">
                            <nop-label asp-for="SearchWidgetZoneName" />
                        </div>
                        <div class="col-md-8">
                            <nop-editor asp-for="SearchWidgetZoneName" />
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-md-4">
                            <nop-label asp-for="SearchWidgetZoneSystemName" />
                        </div>
                        <div class="col-md-8">
                            <nop-editor asp-for="SearchWidgetZoneSystemName" />
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="text-center col-sm-12">
                            <button type="button" id="search-widget-zones" class="btn btn-primary btn-search">
                                <i class="fas fa-search"></i>
                                @T("Admin.Common.Search")
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>